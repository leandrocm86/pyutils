[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyutils"
version = "0.2.0"
description = "Personal utility python modules"
requires-python = ">=3.11"

[project.optional-dependencies]
test = [
    "pytest"
]

[tool.hatch.build.targets.wheel]
packages = ["utils", "std"]
